# Arcade Swarm Supervisor — Python dependencies
# Install with: pip install -r requirements.txt

# Process management and subprocess orchestration
subprocess32==3.5.4; python_version < "3.2"

# YAML config parsing
pyyaml>=6.0

# Structured logging
structlog>=24.0

# CLI interface
click>=8.1

# Async task coordination
asyncio-pool>=0.6
