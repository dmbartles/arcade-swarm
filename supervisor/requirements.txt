# Arcade Swarm Supervisor — Python dependencies
# Install with: pip install -r requirements.txt

# Anthropic SDK — direct API access for agent orchestration
anthropic>=0.40.0

# Load ANTHROPIC_API_KEY from .env file
python-dotenv>=1.0.0

# YAML config parsing
pyyaml>=6.0

# Structured logging
structlog>=24.0

# CLI interface
click>=8.1

# Async task coordination
asyncio-pool>=0.6
