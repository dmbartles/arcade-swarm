/**
 * main.ts — Phaser game entry point.
 *
 * This file is owned by the engine agent. It is scaffolded here as a minimal
 * stub so the Vite build has a valid entry point and `npm run build` succeeds
 * before the engine agent's implementation is merged.
 *
 * The engine agent will replace this with the full Phaser.Game configuration,
 * scene registration, and boot logic.
 *
 * DO NOT add game logic here — this stub is intentionally minimal.
 */

// Minimal stub — replaced by engine agent.
// Exists solely to give Vite a valid TypeScript entry point.
console.info('[Missile Command Math] Engine not yet initialised — awaiting engine agent.');
